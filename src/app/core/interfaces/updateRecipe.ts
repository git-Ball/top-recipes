export interface IUpdateRecipe {

    recipeName: string,
    img: string,
    ingredients: string,
    preparation: string,
// ownerInfo:UserService['currentUser'];

    // owner: string;
    // _id:IUser;
    // __v:number;
}